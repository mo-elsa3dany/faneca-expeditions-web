generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Lead {
  id          String   @id @default(cuid())
  email       String
  name        String?
  quizJson    String
  createdAt   DateTime @default(now())
}
